# GitHub Copilot Instructions

## Project overview
TODO: one paragraph description of what this codebase does.

## Stack
TODO: e.g. Next.js 14 (App Router), TypeScript, PostgreSQL, Prisma ORM, Tailwind CSS.

## Coding conventions
- TODO: e.g. "Prefer server components; only add 'use client' when strictly needed"
- TODO: e.g. "Use named exports — no default exports"
- TODO: e.g. "All API routes return { data } on success and { error: string } on failure"
- TODO: e.g. "Never use `any` — use `unknown` and narrow with type guards"

## Testing
- TODO: e.g. "Tests live in src/__tests__/ and use Vitest"
- TODO: e.g. "Mock external APIs; never mock the database"

## What to avoid
- TODO: e.g. "Don't add console.log — use structured logging via lib/logger.ts"
- TODO: e.g. "Don't import from node_modules paths directly — use the lib/ wrappers"

## Auth
TODO: how auth works in this project.
