# Genbrugelig deploy for Ferm-apps — kaldes fra hver apps deploy.yml via
# `uses: fermrad/AI-tools/.github/workflows/deploy-app.yml@main`.
#
# Findes fordi juni-julis deploy-udbygning kostede den samme fejlrettelse 4-5
# gange — én gang pr. app (drop -dev-suffix, NPM_TOKEN-injektion, vendor-sync,
# SSH-keepalive, public/-sync, GIT_SHA-resolution ...). Workflows var copy-pastet,
# så hver lærdom skulle genopdages i hvert repo. Her rettes den én gang.
#
# Triggerne (`on: push` med path-filtre, workflow_dispatch) bliver i den
# kaldende app — de ER app-specifikke. Alt andet parameteriseres.
#
# Tre deploy-strategier (vælges med `deploy_strategy`):
#   up-build       — compose-fil pr. miljø; `up -d --build --force-recreate
#                    --remove-orphans`; migrationer bagefter i app-containeren.
#                    (projects model — kræver eksplicit prod-compose-fil, jf.
#                    project #105/#106: orphan-oprydning sletter ellers alt
#                    der ikke står i filen.)
#   build-up       — én compose-fil; `build` (prod: --no-cache --pull, ellers
#                    --pull) og `up -d` (prod: --force-recreate) hver for sig;
#                    migrationer bagefter i app-containeren. (crm/area51.)
#   migrate-first  — byg imaget, (valgfrit) skema-drift-gate, migrér i en
#                    engangscontainer, kør app-repoets SQL-scripts, og genstart
#                    FØRST derefter — den nye kode møder aldrig en gammel
#                    database, og et tabsgivende skema stopper deployet før
#                    noget er rørt. (devhubs model, jf. devhub #25 og
#                    docs/deploy-migrations.md — den bedste; nye apps bør vælge
#                    den.)
#
# ── Hvor imaget bygges (`build_mode`) ───────────────────────────────────────
#
# Strategien ovenfor bestemmer RÆKKEFØLGEN; `build_mode` bestemmer HVOR imaget
# bygges. De to er uafhængige.
#
#   on-host   — default og hidtidig adfærd: `docker compose build` kører på
#               MÅLSERVEREN over ssh.
#   registry  — imaget bygges på runneren, pushes til ghcr.io med commit-SHA'en
#               som tag, og serveren `pull`er det.
#
# Hvorfor `registry` findes (project S-349, målt i prod 15-08-2026): et byg på
# målserveren udsulter den kørende app. Målingen var 13 minutters TOTALT udfald
# (502 på alt, også forsiden), mens kørslen stod på `Build image` — altså FØR
# `Start containers`. Nedetiden var altså ikke genstarten; den var byggeriet, og
# den kom uanset om udrulningen indeholdt en skemaændring eller kun en
# tekstrettelse. Samme klasse som area51/project S-320 på staging-boksen.
#
# `migrate-first`s løfte — "miljøet er urørt, når porten siger nej" — gælder
# SKEMAET, ikke boksen. `registry` er dét, der får løftet til også at gælde
# maskinen.
#
# Fem ting, `registry` kræver af app-repoet:
#   * Compose-servicen skal have `image: ${APP_IMAGE:-<lokalt navn>}` ved siden
#     af sin `build:`. `:-` er ikke pynt: i on-host-tilstand er APP_IMAGE tom,
#     og servicen skal da falde tilbage på det lokale navn.
#   * `GIT_SHA`/`GIT_REF` skal stå i compose-filens `environment:`, ikke kun som
#     build-args. Ét image pr. SHA genbruges på tværs af miljøer, så en værdi,
#     der er BAGT ind, ville rapportere det første bygs ref for alle senere
#     udrulninger af samme commit. Runtime-env er allerede dét, appen læser.
#   * Kaldejobbet skal have `permissions: { contents: read, packages: write }`.
#     GITHUB_TOKEN i en genbrugsworkflow arver kaldejobbets rettigheder, og uden
#     `packages: write` kan pushet ikke ske.
#   * Et NEXT_PUBLIC_*-flag, der læses i en KLIENTkomponent, inlines ved byg og
#     kan derfor ikke variere pr. miljø, når ét image deles. Læses det kun
#     server-side (som project gør), er det runtime-env og upåvirket. Tjek det,
#     før en app skifter — det ser ens ud i koden og er det ikke.
#   * BYGGET MÅ IKKE HAVE BRUG FOR EN HEMMELIGHED (S-354 punkt 3). On-host-
#     vejen sender `NPM_TOKEN` med ind i `docker compose build`; registry-vejen
#     sender INGENTING. Der er ikke bygget en mekanisme til det: build-args
#     interpoleres synligt i kørselsloggen og følger med i imagets metadata, så
#     en hemmelighed dér er ikke en løsning, og `secrets:` +
#     `--mount=type=secret` i Dockerfilen findes ikke i huset endnu.
#
#     I dag rammer ÉN app npm-registret under bygget: **crm**. `app/.npmrc`
#     peger `@fermrad` mod npm.pkg.github.com med `_authToken=${NPM_TOKEN}`, og
#     `app/Dockerfile` erklærer `ARG NPM_TOKEN`. Sætter man `build_mode:
#     registry` på crm, fejler `npm install` med 401 — og fejlen ligner et
#     udløbet token, ikke en manglende mekanisme. **crm kan altså ikke skifte
#     endnu.**
#
#     Bemærk fælden i den anden retning: project HAR også et `.npmrc` med
#     `_authToken=${NPM_TOKEN}`, men vendorer sine `@fermrad`-pakker, så intet
#     opløses mod registret under bygget. Filens tilstedeværelse er derfor
#     IKKE svaret på, om en app kan skifte — det er, om bygget faktisk henter
#     noget, der kræver et token.
#
# `registry` kan ikke kombineres med `up-build`: dén strategi bygger på boksen
# som en del af sit `up`. Kombinationen afvises i et eget trin frem for at
# bygge på serveren i tavshed.
#
# ── Diskvagt og oprydning (project S-362) ───────────────────────────────────
#
# `registry` købte nedetiden ned og betalte med DISK. Vejen henter et NYT image
# pr. SHA, og intet fjernede det gamle: ti udrulninger på én dag gav 125 images
# og 29,4 GB på projects staging-boks, som nåede 100 % og væltede udrulningen af
# `f25a38e` 16-08-2026 — ikke i genstarten, men i `Run SQL scripts`, med
# `could not extend file "base/16384/20851": No space left on device`.
#
# Trinnet `Diskvagt og oprydning` ligger derfor FØR alt, der rører boksen:
# før backup, før rsync, før pullet, før migrationerne, før genstarten. Det er
# ikke en detalje om placering, det er hele virkningen — det er PULLET, der
# fylder disken, så en oprydning bagefter kommer altid et deploy for sent, og
# et deploy, der dør på "no space", har ikke ryddet noget.
#
# Trinnet gør tre ting, og de er tre forskellige beslutninger:
#
#   1. IMAGES — `image_keep` UBRUGTE af appens EGET image-repo beholdes, og alt
#      andet fjernes. Bemærk skellet: "de N nyeste" er ikke det samme som "den
#      KØRENDE plus N". En ren alders- eller antalsregel ville rydde det image,
#      containeren kører på, på en boks der ikke er udrullet i en uge. Trinnet
#      spørger derfor MASKINEN — `docker ps -aq` → `docker inspect .Image` — og
#      fredner hvert image-ID, en container (kørende ELLER stoppet) hænger på,
#      før den overhovedet begynder at tælle. En stoppet container er en
#      rollback-sti, der er valgt bevidst (jf. `docker start` som vejen tilbage
#      ved en pensionering), og den skal beholde sit image.
#
#      Snittet er appens eget repo og ikke `docker image prune -a`. Boksene er
#      DELTE — dev kører project + crm + area51, staging bærer også ferm-os og
#      devhub — så en global prune ville lade den enes udrulning fjerne den
#      andens rollback-images. Fjernelsen sker med `docker image rm <ref>` og
#      ikke med `prune`, så loggen kan opregne præcis, hvad der forsvandt.
#
#      Er `build_mode: on-host` (default), er der intet SHA-tagget repo at
#      rydde i, og punktet gør ingenting.
#
#   2. HÆNGENDE IMAGES — `docker image prune -f` UDEN `-a`. Kun utaggede
#      images uden en container. Det er dødvægten fra et on-host-byg, hvor
#      tagget flyttede til den nye udgave, og det er derfor det ene punkt, der
#      også gavner en app, som ikke har valgt registry til. `prune` rører pr.
#      definition ikke et image, en container hænger på.
#
#   3. BYGGECACHE — to bokse, to regler, fordi cachen betyder to forskellige
#      ting de to steder.
#
#      `build_mode: registry` → `docker builder prune -af`. En registry-boks
#      bygger ALDRIG, så cachen er død vægt fra fødslen (prod bar 10,25 GB af
#      den slags). Uændret siden S-362 — "Registry-udrulningen har ingen
#      oprydning — staging-boksen løb tør for disk efter ti deploys".
#
#      `build_mode: on-host` → `docker builder prune -f --filter
#      until=<builder_keep_hours>h`, og **kun hvis appen har sat inputtet**.
#      Default er `0`, altså SLUKKET: fire apps kalder denne fil, og en ny
#      oprydning må ikke ændre adfærd for nogen, der ikke har bedt om den.
#      Et ubetinget `-af` ville gøre mest skade netop her — dev-boksen er
#      2 vCPU / 3,8 GB uden swap og kører dev for project + crm + area51
#      samtidig, og S-291 — "Dev-boksen har 3,8 GB uden swap og OOM'er, når to
#      apps bygger samtidig" — målte OOM-dræberen slå til otte gange på under
#      to timer, hvorefter tailscaled mistede sin rute. Et fuldt genbyg ved
#      hvert deploy er præcis den belastning.
#
#      ⭐ Hvorfor ALDER og ikke et loft (`--max-used-space`), som S-365 — "De
#      interne bokse bærer 42 GB byggecache — S-362's oprydning rører dem med
#      vilje ikke" — også overvejede. Tre målinger, alle 20-08-2026:
#
#        * `until=` måler på hvornår posten SIDST BLEV BRUGT, ikke på hvornår
#          den blev skabt. Efterprøvet mod en rigtig dæmon i
#          `scripts/test-deploy-diskvagt-byggecache.sh`: en post, der blev
#          SKABT før snittet, men GENBRUGT af et byg efter det, overlever.
#          En cachepost, byggene stadig rammer, nulstiller altså sit eget ur og
#          ældes aldrig ud. Det er dét, der gør reglen hård uden at ramme det
#          varme.
#        * Et loft rammer efter STØRRELSE og er blindt for, om offeret er
#          varmt. På intern staging var 7,55 GB af cachen sidst brugt for få
#          minutter siden; et loft på 5 GB ville have kastet ~2,5 GB af netop
#          dét væk — de lag, næste byg skulle genbruge. Aldersreglen beholder
#          dem alle og henter stadig 10,51 GB.
#        * Og `--keep-storage`, som punktet foreslog, FINDES IKKE længere på
#          boksene: Docker 29.6/29.5 kender kun `--reserved-space` /
#          `--max-used-space`. Med oprydningens `|| true` ville flaget være
#          blevet en tavs no-op.
#
#      Målt 20-08-2026 bærer de to interne bokse 23,15 GB (169 poster, hver
#      eneste sidst brugt for 3-4 uger siden) og 18,07 GB (155 poster, hvoraf
#      10,51 GB er ubrugt i over en uge).
#
# Diskvarslet ligger i det SAMME trin, og rækkefølgen inde i det er valgt:
# der måles FØR oprydningen, ryddes, og måles IGEN — og varslet udtaler sig om
# det, der er tilbage BAGEFTER. Målte man kun før, ville varslet råbe på en
# boks, oprydningen lige har reddet; ryddede man først og målte kun bagefter,
# ville man ikke kunne se, om oprydningen overhovedet gjorde noget. Begge tal
# står i loggen.
#
# Tærsklerne er `disk_warn_percent` (advarsel) og `disk_fail_percent` (stop),
# begge STRENGT større end. Målt 16-08-2026 stod alle fire bokse under
# advarselstærsklen — prod 73 %, staging 76 %, intern prod 79 %, intern staging
# 80 % — så defaults er sikre for en app, der ikke har valgt noget til.
# `disk_fail_percent: 100` slår stoppet fra uden at slå målingen fra.
#
# Vagten FEJLER LUKKET: kan målingen ikke læses ud af svaret, stopper deployet
# frem for at fortsætte i tro på en tom disk-oplysning.
#
# SQL, som Prisma ikke ejer: `scripts/sql/*.sql`
#
# `prisma db push` kender ikke CHECK-constraints og triggere, og
# skema-drift-gaten ser dem heller ikke. De overlever derfor ethvert deploy —
# men de kommer aldrig af sig selv, så et nyt miljø manglede dem, indtil nogen
# huskede at køre dem i hånden. (Indeks er en anden sag: dem MODELLERER Prisma,
# så et håndlavet indeks, der ikke står i schema.prisma, kan `db push` finde på
# at droppe igen. Læg ikke indeks her uden at have tjekket netop det.)
#
# Kontrakten med app-repoet:
#   * Mappen findes på SERVEREN som `<app_dir>/scripts/sql/`, altså efter
#     rsync. For apps med `sync_path: app/` betyder det `app/scripts/sql/` i
#     repoet. Ligger filerne forkert, er trinnet en tavs no-op.
#   * Filerne køres i navnesorteret rækkefølge (LC_ALL=C), så 010-, 020- styrer
#     rækkefølgen.
#   * De køres ved HVERT deploy, i alle miljøer, og SKAL derfor være
#     idempotente (`if not exists ...`) og additive. Hver fil køres som én
#     transaktion, så en fil enten går helt igennem eller slet ikke.
#   * Ingen mappe = no-op. Ingen app i org'en havde mappen, da trinnet blev
#     indført, så det er inert, indtil nogen tager det i brug.
#   * App-repoets EGET `paths`-filter skal dække mappen, ellers udløser en ren
#     SQL-ændring intet deploy: den bliver merged grøn og ruller aldrig ud.
#     `scripts/**` dækker den (devhub, area51); `app/**` gør det for apps med
#     `sync_path: app/` (crm). Har filteret hverken, skal `scripts/sql/**`
#     tilføjes eksplicit — det manglede i project og ramte project#173 samme
#     dag, mappen blev taget i brug dér. Trinnet kan ikke selv fange det:
#     filteret bestemmer, om workflowet overhovedet starter.
#   * Trinnet rammer DB-containeren med `docker exec`, ikke gennem compose, så
#     den skal hedde det rigtige i HVERT miljø. `db_container` er prod-navnet;
#     afviger dev eller staging, skal `dev_db_container` /
#     `staging_db_container` sættes. Gør de ikke det, fejler deployet med
#     containernavnet i beskeden — det ramte projects staging og dev, indtil
#     de to inputs fandtes. Backup-trinnet vælger container efter samme regel
#     (det gjorde det IKKE før S-211, se kommentaren ved "Backup database").
#
# Secrets kommer via `secrets: inherit`. Host-secrets kan re-mappes pr. app
# (area51 bor på de interne bokse, devhubs dev på den offentlige).
#
# ── Citering ind i en fjern shell (S-354 punkt 2) ───────────────────────────
#
# Hver `ssh … "<kommando>"` her sender en STRENG. ssh(1) samler sine
# restargumenter med mellemrum imellem, og sshd kører resultatet som
# `$SHELL -c <streng>`. Værdien parses altså EN GANG TIL, på den anden maskine,
# som root — og runnerens egne anførselstegn er væk, længe før det sker.
#
# Det var derfor `export GIT_REF='${GIT_REF}'` var et hul og ikke bare grimt.
# `GIT_REF` er appens branchnavn (`github.ref_name`, eller `inputs.ref` ved en
# dispatch), og et branchnavn MÅ indeholde `'`: `git check-ref-format --branch
# "feat/x'\$(whoami)'y"` siger god. Værdien lukkede den enkeltcitering, den
# selv stod i, og resten kørte som root på boksen. Målt 18-08-2026 på alle tre
# on-host-kaldesteder.
#
# To rettelser, der IKKE virker, og hvorfor:
#   * `<< 'EOF'` / hårde enkeltcitationstegn omkring hele kommandoen: så holder
#     `${GIT_SHA}`, `${COMPOSE_FLAGS}` og `${APP_IMAGE}` også op med at
#     ekspandere, og udrulningen brækker for alle apps.
#   * `ssh … bash -s -- "$GIT_REF"`: ssh samler stadig argumenterne til én
#     streng, som login-shellen parser. Injektionen overlever.
#
# Det, der virker: cite værdien til bash-genparsning, FØR den sendes.
#
#     Q_GIT_REF=$(printf '%q' "$GIT_REF")
#     ssh … "… export GIT_REF=${Q_GIT_REF} …"
#
# `%q` udskriver en streng, bash læser tilbage som præcis den samme streng —
# uden anførselstegn omkring, for `%q` leverer sin egen citering. `${VAR}` og
# `${Q_VAR}` ekspanderer begge på RUNNEREN som før; forskellen er kun, hvad den
# fjerne shell så får at parse. Tom værdi bliver til `''`, så `APP_IMAGE=''` i
# on-host-tilstand er uændret.
#
# Og kommer kroppen ALLIGEVEL ind på stdin (`ssh … bash -s`), findes der en
# stærkere form end at citere ind i kommandostrengen: send værdierne som
# NAVNGIVNE tildelinger FORAN kroppen på stdin, og lad selve ssh-kommandoen
# være konstant.
#
#     { printf 'IMAGE_REPO=%q\n' "$IMAGE_REPO"
#       cat << 'REMOTE'
#       …
#     REMOTE
#     } | ssh … "root@${HOST}" bash -s
#
# Så har `$SHELL -c <streng>` slet ingen værdi at parse — værdien når aldrig
# den shell. Og dét, der fældede diskvagten (S-447): en TOM værdi kan ikke
# forsvinde. Den bliver til `IMAGE_REPO=''` på sin egen linje frem for et
# argument, som ssh's fladning taber, hvorefter alle de følgende argumenter
# rykker en plads ned og `$4` aldrig bliver sat. `ssh … bash -s -- "$A" "$B"`
# er altså ikke bare svagere mod injektion; den kan TABE et argument, og den
# binder på PLADS frem for på NAVN, så hver ny værdi omnummererer de gamle.
#
# Samme form bruges i `fermrad/project`s `deploy-pr-stack.yml` (S-411), så der
# kun findes én måde at gøre det rigtigt på i huset.
#
# Værdier, der bevidst IKKE citeres: `COMPOSE_FLAGS`, `BUILD_FLAGS`, `UP_FLAGS`.
# De er FLERE argumenter (`-f fil`) og skal splittes af den fjerne shell — de
# sættes af workflowet selv og bærer ikke inddata.

name: Deploy Ferm app

on:
  workflow_call:
    inputs:
      environment:
        description: 'dev | staging | production'
        required: true
        type: string
      checkout_ref:
        description: 'Commit/branch der checkes ud og deployes'
        required: true
        type: string
      git_ref:
        description: 'Ref-navn appen rapporterer til DevHub via /api/health'
        required: true
        type: string
      app_slug:
        description: 'Kort navn — backup-filnavne og DevHub-rapportering (project, crm, ...)'
        required: true
        type: string
      app_dir:
        description: 'Deploy-mappe på serveren, fx /opt/ferm-project'
        required: true
        type: string
      sync_path:
        description: 'Lokal sti der rsync''es (crm har koden i app/)'
        required: false
        type: string
        default: '.'
      extra_excludes:
        description: 'Ekstra rsync-excludes, mellemrumssepareret'
        required: false
        type: string
        default: ''
      db_container:
        description: 'Postgres-containerens navn (til backup)'
        required: true
        type: string
      db_user:
        required: true
        type: string
      db_name:
        required: true
        type: string
      app_container:
        description: 'App-containerens navn (til migrations ved exec-strategierne)'
        required: true
        type: string
      staging_app_container:
        description: 'App-containernavn på staging, hvis det afviger'
        required: false
        type: string
        default: ''
      # db_container er PROD-navnet. Miljøerne har typisk hver sit — hos project
      # ferm-project-db (dev), ferm-project-staging-db (staging) og
      # ferm-projects-db (prod) — og SQL-trinnet rammer containeren direkte med
      # docker exec, ikke gennem compose. Uden de her to kørte det med prod-navnet
      # i alle miljøer og fejlede med "No such container" på dev og staging.
      # De bruges af BÅDE backup-trinnet og SQL-trinnene. Backup'en gjorde det
      # ikke oprindeligt: den brugte prod-navnet i alle miljøer og sprang tavst
      # over, fordi "container findes ikke" dér var en overspringning frem for
      # en fejl (S-211).
      staging_db_container:
        description: 'DB-containernavn på staging, hvis det afviger'
        required: false
        type: string
        default: ''
      dev_db_container:
        description: 'DB-containernavn på dev, hvis det afviger'
        required: false
        type: string
        default: ''
      compose_service:
        description: 'Compose-servicenavn for appen (til migrate-first run --rm)'
        required: false
        type: string
        default: 'app'
      compose_file_dev:
        required: false
        type: string
        default: 'docker-compose.dev.yml'
      compose_file_staging:
        required: false
        type: string
        default: 'docker-compose.staging.yml'
      compose_file_production:
        # Default docker-compose.yml er typisk LOKAL DEV (kun db). Uden eksplicit
        # prod-fil slettede --remove-orphans den håndkørte prod-app som orphan
        # (project #105, incident 2026-07-17). Derfor er prod-filen et krav om
        # at være eksplicit — ingen falder tilbage til docker-compose.yml.
        required: false
        type: string
        default: 'docker-compose.production.yml'
      deploy_strategy:
        description: 'up-build | build-up | migrate-first (se toppen af filen)'
        required: false
        type: string
        default: 'up-build'
      build_mode:
        # on-host er den hidtidige adfærd og forbliver default — ingen app
        # skifter, før den selv beder om det.
        #
        # `registry` sender INGEN byggetids-hemmeligheder (S-354 punkt 3). En
        # app, hvis byg henter fra et privat npm-register, kan derfor ikke
        # skifte endnu — crm er den ene i huset i dag. Se femte punkt under
        # "Hvor imaget bygges" øverst.
        description: 'on-host | registry — hvor imaget bygges (se "Hvor imaget bygges" øverst)'
        required: false
        type: string
        default: 'on-host'
      image_name:
        description: 'Registry-image UDEN tag. Tom = ghcr.io/<owner>/<repo> for det KALDENDE repo.'
        required: false
        type: string
        default: ''
      build_args:
        # GIT_SHA og GIT_REF sættes altid af workflowet selv — skriv dem ikke her.
        description: 'Ekstra build-args ved registry-byg, én KEY=VALUE pr. linje'
        required: false
        type: string
        default: ''
      image_keep:
        # UBRUGTE images, ikke images i alt: alt hvad en container (kørende
        # eller stoppet) hænger på, fredes FØR der tælles. 3 er valgt, fordi en
        # tilbagerulning skal kunne ske uden at hente over nettet, og fordi
        # projects image er ~1,7 GB — tre ubrugte er ~5 GB, hvor ti var ~17 GB.
        description: 'Ubrugte images af appens eget repo, der beholdes som rollback-sti (kun build_mode: registry)'
        required: false
        type: number
        default: 3
      builder_keep_hours:
        # SLUKKET som default, og det er en bevidst beslutning frem for et
        # forsigtigt tal: fire apps kalder denne fil, og byggecachen er dét,
        # en on-host-boks bygger hurtigt af. Reglen gælder først, når en app
        # selv har sagt ja. 168 (en uge) er det tal, målingerne i afsnittet
        # "Diskvagt og oprydning" øverst i filen er regnet på.
        #
        # Snittet er SIDST BRUGT, ikke skabt — en cachepost, byggene stadig
        # genbruger, nulstiller sit eget ur og ældes aldrig ud.
        #
        # Ignoreres ved `build_mode: registry`: dér bygges der aldrig, og
        # cachen ryddes helt (`-af`) som hidtil.
        description: 'Ryd byggecache, der ikke har været brugt i så mange timer (0 = slået fra; kun build_mode: on-host)'
        required: false
        type: number
        default: 0
      disk_warn_percent:
        description: 'Advar, når disken efter oprydningen er over denne procent'
        required: false
        type: number
        default: 85
      disk_fail_percent:
        # 100 slår stoppet fra uden at slå målingen fra: sammenligningen er
        # strengt større end, og en disk kan ikke komme over 100 %.
        description: 'Stop deployet, når disken efter oprydningen er over denne procent (100 = aldrig)'
        required: false
        type: number
        default: 95
      schema_drift_gate:
        description: 'Stop deployet hvis skemaændringen er tabsgivende (kræver scripts/schema-drift.mjs i app-repoet; kun migrate-first)'
        required: false
        type: boolean
        default: false
      tailscale:
        description: 'always | dev-only | never — hvornår runneren skal på tailnettet'
        required: false
        type: string
        default: 'always'
      dev_host_secret:
        description: 'Navn på org-secret med dev-boksens host'
        required: false
        type: string
        default: 'INTERNAL_DEV_SSH_HOST'
      staging_host_secret:
        required: false
        type: string
        default: 'STAGING_SSH_HOST'
      production_host_secret:
        required: false
        type: string
        default: 'PRODUCTION_SSH_HOST'
      reload_caddy:
        description: 'never | non-dev | always — genindlæs ferm-caddy efter deploy'
        required: false
        type: string
        default: 'never'
      ensure_dev_dir:
        description: 'Sikr app-mappe + symlink på dev-boksens persistente volume'
        required: false
        type: boolean
        default: true

jobs:
  # ── Ref-vagten (project S-452) ────────────────────────────────────────────
  #
  # Fælder en FORKORTET commit-SHA i `checkout_ref`, før `actions/checkout`
  # når at gætte forkert på den.
  #
  # Målt 20-08-2026 på en prod-dispatch af project med `ref=1c80196`:
  # checkout slog den korte SHA op som et GRENNAVN — `+refs/heads/1c80196` —
  # og gav tre forsøg over 35 sekunder, der alle endte i
  # `The process '/usr/bin/git' failed with exit code 1`. Intet i den fejl
  # nævner refs, grene eller SHA-længde; man skal ind i loggen OVER fejlen og
  # læse `refs/heads/` for at se, hvad der gik galt. Genkørslen med den fulde
  # 40-tegns SHA gik igennem.
  #
  # Vagten er en FÆLDE og ikke en hjælpsomhed. Trinnet kunne folde den korte
  # SHA ud til den fulde (`gh api repos/OWNER/REPO/commits/REF -q .sha`), men
  # den, der pinner et prod-deploy, skal se præcis den SHA, der rulles ud —
  # ikke få en anden end den, hun skrev.
  #
  # ── Hvorfor vagten bor HER og ikke i hver apps deploy.yml ─────────────────
  #
  # Punktet foreslog trinnet i `deploy.yml`. Det kan ikke lade sig gøre som et
  # trin: `jobs.deploy` i alle fire apper er et job, der KALDER en
  # genbrugsworkflow, og GitHub tillader kun name/uses/with/secrets/strategy/
  # needs/if/concurrency/permissions på sådan et job — der er ingen `steps:`
  # at hænge vagten på. Den ville altså kræve et NYT job i hvert af de fire
  # repos, altså fire kopier af den samme regex og den samme besked. Her står
  # den ét sted og dækker project, crm, devhub og area51 på én gang.
  #
  # Prisen er, at hver app skal bumpe sit pin til denne fil, før vagten når
  # frem. Det er den samme pris som enhver anden rettelse her, og den er
  # bevidst (se pin-kommentaren i app-repoernes deploy.yml).
  #
  # ── Eget job og ikke et trin i `build`/`deploy` ───────────────────────────
  #
  # BEGGE de to jobs nedenfor checker ud med `checkout_ref`, så et trin skulle
  # skrives to gange i denne fil for at dække begge veje. Et eget job siger
  # det én gang — og fælder som en NAVNGIVEN rød boble, hvilket er hele
  # pointen i et punkt om en ulæselig fejl.
  #
  # Job-id'et er `ref_gate` med UNDERSTREG. `needs.ref-gate.result` ville
  # blive læst som et minus af Actions' udtrykslag.
  ref_gate:
    name: Kontrollér ref
    runs-on: ubuntu-latest
    steps:
      # Værdien læses som `"$REF"` fra `env:` og interpoleres IKKE ind i
      # kommandoen. `${{ }}` substitueres af Actions, før shellen ser
      # `run:`-blokken — det er dét, S-411/S-418 kostede huset 20 tomme
      # kørsler at lære, og actionlint-porten på project vogter netop det.
      - name: En forkortet SHA er ikke en commit for actions/checkout
        env:
          REF: ${{ inputs.checkout_ref }}
        run: |
          set -euo pipefail
          # 7-39 hexcifre og intet andet. 40 er den gyldige form og slipper
          # forbi; alt med en skråstreg (`refs/heads/...`) gør det også, og
          # dét er med vilje — se den falske positiv nedenfor.
          if printf '%s' "$REF" | grep -qE '^[0-9a-fA-F]{7,39}$'; then
            echo "::error title=Forkortet SHA i ref::'$REF' ligner en forkortet commit-SHA, og actions/checkout kan ikke checke sådan en ud. Alt kortere end 40 tegn slås op som et GRENNAVN (refs/heads/$REF) og fejler efter tre forsøg med 'The process /usr/bin/git failed with exit code 1' — en fejl, der ikke nævner refs med et ord. Brug den FULDE 40-tegns SHA: git rev-parse $REF, eller gh api repos/OWNER/REPO/commits/$REF -q .sha. Er '$REF' derimod et rigtigt grennavn eller tag, så skriv det ud som refs/heads/$REF eller refs/tags/$REF — begge slipper forbi denne vagt og er samtidig dét, der gør det entydigt for git."
            exit 1
          fi
          echo "ref '$REF' er ikke en forkortet SHA — videre."

  # ── Byg og push af imaget — kører KUN på runneren (S-354 punkt 1) ──────────
  #
  # Bygget bor i sit EGET job, fordi `packages: write` ellers fulgte med ind i
  # de trin, der SSH'er til målserveren. GITHUB_TOKEN i en genbrugsworkflow
  # arver kaldejobbets rettigheder, og en rettighed kan ikke skrues ned pr.
  # TRIN — kun pr. job. Så længe byg og udrulning lå i samme job, sendte
  # `Pull image on server` altså et SKRIVE-dygtigt registry-token til en boks,
  # der kun skal kunne LÆSE.
  #
  # Her har byg-jobbet ingen `permissions:`-blok (det arver kaldejobbets, som
  # før, og skal have `packages: write` for at kunne pushe), mens deploy-jobbet
  # nedenfor skruer ned til `packages: read`.
  build:
    needs: [ref_gate]
    if: inputs.build_mode == 'registry'
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.image.outputs.image }}
      image_repo: ${{ steps.image.outputs.image_repo }}
      git_sha: ${{ steps.sha.outputs.git_sha }}
    env:
      GIT_REF: ${{ inputs.git_ref }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          ref: ${{ inputs.checkout_ref }}

      # github.sha er tom ved workflow_dispatch (måden prod deployes på), så
      # GIT_SHA ville blive branch-navnet i stedet for commit'en. Læs derfor den
      # faktisk udcheckede commit, så /api/health rapporterer korrekt SHA til DevHub.
      - name: Resolve deployed commit SHA
        id: sha
        run: |
          SHA="$(git rev-parse HEAD)"
          echo "GIT_SHA=$SHA" >> "$GITHUB_ENV"
          echo "git_sha=$SHA" >> "$GITHUB_OUTPUT"

      # Kombinationen kan ikke lade sig gøre: up-build bygger på boksen som en
      # del af sit `up -d --build`, altså præcis dét, registry findes for at
      # undgå. Sig fra HER frem for at bygge på serveren i tavshed — en
      # opsætningsfejl, der ligner en virkende udrulning, er den dyre slags.
      - name: Gate — registry-byg kræver en strategi, der ikke bygger på boksen
        if: inputs.deploy_strategy == 'up-build'
        run: |
          echo "::error title=build_mode=registry kan ikke kombineres med deploy_strategy=up-build::up-build kører 'docker compose up -d --build' på målserveren og bygger altså imaget dér, uanset at det også er bygget og pushet i CI. Vælg build-up eller migrate-first."
          exit 1

      # Imagets navn OG tag. Tagget er commit-SHA'en — S-349 kræver, at imaget
      # versioneres pr. SHA, så en udrulning kan pege på præcis den commit, den
      # bygger på, og en tilbagerulning kan hente et image, der allerede findes.
      #
      # ghcr kræver småt image-navn; org- og repo-navne må gerne have store
      # bogstaver, så navnet foldes ned frem for at fejle inde i docker.
      - name: Resolve image reference
        id: image
        run: |
          NAME="${{ inputs.image_name }}"
          NAME="${NAME,,}"   # ghcr kræver småt; fold BEGGE veje, ikke kun default-navnet
          if [ -z "$NAME" ]; then
            # github.repository er det KALDENDE repo (fermrad/project), ikke
            # AI-tools — genbrugsworkflows kører i kalderens kontekst. Det er
            # også dét, der gør, at kalderens GITHUB_TOKEN kan pushe pakken.
            NAME="ghcr.io/${GITHUB_REPOSITORY,,}"
          fi
          echo "APP_IMAGE=${NAME}:${GIT_SHA}" >> "$GITHUB_ENV"
          # …og som job-output, fordi deploy-jobbet er et ANDET job nu
          # (se noten om `packages: write` øverst). $GITHUB_ENV krydser
          # ikke en jobgrænse; det gør outputs.
          echo "image=${NAME}:${GIT_SHA}" >> "$GITHUB_OUTPUT"
          # Repo-navnet UDEN tag. Oprydningen (S-362) skal kunne opregne appens
          # ØVRIGE tags på boksen, og den må ikke udlede navnet ved at klippe i
          # APP_IMAGE: et registry-navn kan bære en port (`host:5000/app`), så
          # "alt før sidste kolon" er en regel, der virker lige indtil den ikke
          # gør. Navnet skrives dér, hvor det allerede er kendt.
          echo "APP_IMAGE_REPO=${NAME}" >> "$GITHUB_ENV"
          echo "image_repo=${NAME}" >> "$GITHUB_OUTPUT"
          echo "Image: ${NAME}:${GIT_SHA}"

      # ── Registry-byg: alt her kører på RUNNEREN ────────────────────────────
      # Placeret FØR tailscale/ssh/backup med vilje. Et byg, der ikke kan lade
      # sig gøre, skal fejle uden at have rørt målserveren overhovedet — i
      # on-host-tilstand var en fejlende build allerede efter rsync, altså efter
      # at koden var lagt ud på boksen.
      # Findes tagget allerede, bygges der IKKE om — der pulles bare.
      #
      # Det er dét, der gør en tilbagerulning til en gammel SHA til en HENTNING
      # af nøjagtig det image, der kørte dengang. Uden tjekket ville
      # rollback-vejen bygge commit'en forfra og PUSHE oven i sit eget tag: ghcr
      # har mutable tags, `FROM node:20-bullseye-slim` er et flydende tag, og
      # `npm ci` opløser mod det levende register — så "rul tilbage til X" ville
      # give "X's kildekode bygget I DAG", ikke det image, der kørte. Det er den
      # slags forskel, man opdager midt i et nedbrud.
      #
      # Sidegevinst: en udrulning, der døde i drift-gaten, efterlader et tag i
      # registret. Uden tjekket ville et senere byg af samme SHA overskrive det
      # med noget andet; med tjekket er tagget stabilt fra første push.
      - name: Does the image already exist?
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Rent læse-opslag mod registret — kræver ingen buildx og intet byg.
          if echo "$GHCR_TOKEN" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin >/dev/null 2>&1 \
             && docker manifest inspect "$APP_IMAGE" >/dev/null 2>&1; then
            echo "IMAGE_EXISTS=true" >> "$GITHUB_ENV"
            echo "$APP_IMAGE findes allerede — springer byg og push over."
          else
            echo "IMAGE_EXISTS=false" >> "$GITHUB_ENV"
            echo "$APP_IMAGE findes ikke — bygges."
          fi

      - name: Set up Buildx
        if: env.IMAGE_EXISTS != 'true'
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Log in to ghcr.io (runner)
        if: env.IMAGE_EXISTS != 'true'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          # Kørslens EGET, kortlivede token — ikke en PAT i en org-secret. Det
          # udløber med kørslen, så der ligger ingen blivende registry-nøgle
          # nogen steder, og rettighederne kommer fra kaldejobbets `permissions`.
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        id: push
        if: env.IMAGE_EXISTS != 'true'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ${{ inputs.sync_path }}
          push: true
          tags: ${{ env.APP_IMAGE }}
          # GIT_SHA/GIT_REF sendes med, så et image, der køres UDEN
          # runtime-env, stadig kan sige hvad det er. Runtime-env vinder —
          # se noten om `environment:` øverst i filen.
          #
          # Læg ALDRIG en hemmelighed her: build-args interpoleres synligt i
          # kørselsloggen og følger med i imagets metadata. Skal et byg bruge en
          # hemmelighed (crm's NPM_TOKEN er det eneste tilfælde i huset i dag),
          # kræver det `secrets:` + `--mount=type=secret` i Dockerfilen — og dét
          # findes ikke her endnu. Det er derfor, crm ikke kan skifte til
          # registry uden videre.
          build-args: |
            GIT_SHA=${{ env.GIT_SHA }}
            GIT_REF=${{ env.GIT_REF }}
            ${{ inputs.build_args }}
          # PRODUKTION BYGGER RENT. On-host-vejen har altid sendt
          # `--no-cache --pull` for prod (se "Build image" nedenfor), og den
          # hygiejne må ikke falde bort, bare fordi bygget flyttede.
          #
          # Uden den ville prod-imaget kunne samles af lag, en dev- eller
          # staging-kørsel har lagt i cachen: `Deploy project` kan dispatches med
          # `environment: dev` og en VILKÅRLIG ref, og med `mode=max` eksporteres
          # hvert mellemlag. Den næste prod-udrulning ville så restore dem — en
          # rute fra en uskyldigt udseende dev-dispatch ind i prod-imaget, hvor
          # selve prod-kørslen ser helt normal ud.
          no-cache: ${{ inputs.environment == 'production' }}
          pull: ${{ inputs.environment == 'production' }}
          # Og cachen er pr. app OG MILJØ. Et fælles scope ville lade de to
          # miljøer skrive i hinandens cache — samme rute som ovenfor, bare uden
          # at nogen behøvede at dispatche noget.
          cache-from: type=gha,scope=${{ inputs.app_slug }}-${{ inputs.environment }}
          cache-to: ${{ inputs.environment == 'production' && '' || format('type=gha,mode=max,scope={0}-{1}', inputs.app_slug, inputs.environment) }}

      # Et fejlet registry-byg har ÉN kendt årsag, der ikke ligner sig selv i
      # loggen: bygget havde brug for en hemmelighed, og registry-vejen sender
      # ingen (S-354 punkt 3). `npm install` svarer da 401, hvilket læses som
      # "tokenet er udløbet" — og nogen bruger en time på at rotere et token,
      # der er fuldstændig fint. Trinnet her koster intet, når bygget går godt,
      # og navngiver afgrænsningen, når det ikke gør.
      - name: Hvorfor bygget måske fejlede
        # Kun når det var SELVE bygget, der faldt — ikke når gaten ovenfor
        # sagde fra af en helt anden grund.
        if: failure() && steps.push.outcome == 'failure'
        run: |
          echo "::error title=Registry-bygget fejlede — husk at det ikke bærer hemmeligheder::build_mode=registry sender INGEN byggetids-hemmeligheder til docker build (ingen NPM_TOKEN, ingen secrets-mount). Henter din Dockerfile noget fra et privat register under bygget, fejler det her med 401/403 — og det er ikke et udløbet token, det er en mekanisme, der ikke findes endnu. crm er den app i huset, det gælder. Vejen videre er build_mode: on-host, indtil secrets-mount er bygget. Se 'Hvor imaget bygges' i AI-tools/.github/workflows/deploy-app.yml."

  deploy:
    needs: [ref_gate, build]
    # Byg-jobbet er SPRUNGET OVER i on-host-tilstand, og et sprunget `needs`
    # ville uden det her tage deploy-jobbet med sig. Betingelsen siger derfor
    # "kør, medmindre bygget FEJLEDE" — ikke `always()`, som ville rulle ud
    # oven på et fejlet byg.
    #
    # `ref_gate` skal stå EKSPLICIT her (S-452). `!cancelled()` er en
    # tilsidesættelse af den normale needs-regel, så uden klausulen ville
    # deploy-jobbet køre videre, selv om ref-vagten netop havde fældet — og
    # vagten ville være pynt.
    if: ${{ !cancelled() && needs.ref_gate.result == 'success' && needs.build.result != 'failure' }}
    runs-on: ubuntu-latest
    # Nedskaleringen (S-354 punkt 1). En genbrugsworkflow kan kun skrue NED,
    # aldrig op: `contents: read` bruges af checkout, `packages: read` er dét,
    # `docker login` på målserveren har brug for. Alle andre scopes bliver
    # `none` i dette job.
    #
    # For kalderen betyder det, at kaldejobbet skal give mindst
    # `contents: read` + `packages: read`. Repo-standarden ("read") gør det;
    # skriver en app sin egen `permissions:`-blok, skal begge stå der.
    permissions:
      contents: read
      packages: read
    env:
      DEPLOY_ENV: ${{ inputs.environment }}
      # Kørende git-ref (branch/tag) — sendes videre til build, så appen kan
      # rapportere referencen (aflæses af DevHub via /api/health). GIT_SHA sættes
      # separat efter checkout, se trinnet nedenfor.
      GIT_REF: ${{ inputs.git_ref }}
      HOST: ${{ inputs.environment == 'production' && secrets[inputs.production_host_secret] || inputs.environment == 'dev' && secrets[inputs.dev_host_secret] || secrets[inputs.staging_host_secret] }}
      # Imagets navn kommer fra byg-jobbet. Er det sprunget over (on-host), er
      # begge tomme — præcis som før, hvor trinnet der satte dem var betinget.
      APP_IMAGE: ${{ needs.build.outputs.image }}
      APP_IMAGE_REPO: ${{ needs.build.outputs.image_repo }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          ref: ${{ inputs.checkout_ref }}

      # github.sha er tom ved workflow_dispatch (måden prod deployes på), så
      # GIT_SHA ville blive branch-navnet i stedet for commit'en. Læs derfor den
      # faktisk udcheckede commit, så /api/health rapporterer korrekt SHA til DevHub.
      #
      # Er der bygget et image, ARVES byggets SHA frem for at læse den
      # forfra: `checkout_ref` kan være et branchnavn, og et push mellem de
      # to jobs ville ellers give en udrulning, der rapporterer én commit
      # og kører imaget af en anden.
      - name: Resolve deployed commit SHA
        env:
          BUILD_SHA: ${{ needs.build.outputs.git_sha }}
        run: |
          if [ -n "$BUILD_SHA" ]; then
            echo "GIT_SHA=$BUILD_SHA" >> "$GITHUB_ENV"
          else
            echo "GIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
          fi

      # Compose-flag pr. miljø én gang for alle trin.
      - name: Resolve compose file
        run: |
          if [[ "$DEPLOY_ENV" == "dev" ]]; then
            echo "COMPOSE_FLAGS=-f ${{ inputs.compose_file_dev }}" >> "$GITHUB_ENV"
          elif [[ "$DEPLOY_ENV" == "staging" ]]; then
            echo "COMPOSE_FLAGS=-f ${{ inputs.compose_file_staging }}" >> "$GITHUB_ENV"
          else
            echo "COMPOSE_FLAGS=-f ${{ inputs.compose_file_production }}" >> "$GITHUB_ENV"
          fi

      - name: Connect to Tailscale
        if: inputs.tailscale == 'always' || (inputs.tailscale == 'dev-only' && inputs.environment == 'dev')
        uses: tailscale/github-action@780049a30b6ff5c378a9e7b389d15ece7a204888 # v4.1.3
        with:
          authkey: ${{ secrets.TAILSCALE_CI_AUTH_KEY }}

      - name: Setup SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          # Retry-loop fra area51: Tailscale-ruten til interne bokse kan være
          # sekunder om at komme op — første keyscan må ikke være dødsdommen.
          # På offentlige bokse rammer første forsøg.
          #
          # Løkken SKAL sige fra, når den løber tør. Uden `exit 1` returnerede
          # trinnet `success`, selv når ruten aldrig kom op — og fejlen dukkede
          # så op ~2 minutter senere som `ssh: connect to host … port 22:
          # Connection timed out` i et trin, der hedder "Ensure app directory
          # exists on persistent volume". Det læser som en disk- eller
          # rettighedsfejl, ikke som en netværksfejl, og kostede reel
          # fejlsøgningstid i project S-290 (kørsel 31412821397).
          for i in $(seq 1 18); do
            ssh-keyscan -T 5 -H "$HOST" 2>/dev/null | tee -a ~/.ssh/known_hosts | grep -q . && break
            if [ "$i" -eq 18 ]; then
              echo "::error title=Ingen rute til $DEPLOY_ENV-boksen::ssh-keyscan nåede aldrig $DEPLOY_ENV-hosten på 18 forsøg (~3 min). Runneren er på tailnettet, men boksen svarer ikke — typisk fordi tailscaled på boksen er sultet af CPU/hukommelse, eller fordi boksen er nede. Se journalctl -u tailscaled på boksen omkring dette tidspunkt."
              exit 1
            fi
            echo "Attempt $i/18 — waiting for route…"
            sleep 5
          done

      # ── Diskvagt og oprydning (S-362) ──────────────────────────────────────
      # FØRSTE trin efter ssh, og det er hele virkningen: alt herefter — backup,
      # rsync, pull, migrationer, genstart — SKRIVER på boksen. Se afsnittet
      # "Diskvagt og oprydning" øverst i filen for de tre oprydningers hver sin
      # betingelse, og for hvorfor der måles BÅDE før og efter.
      #
      # Oprydningen er tålsom (`|| true`): en prune, der fejler, må ikke vælte en
      # udrulning, og gør den det, fanger vagten nedenfor den fulde disk
      # alligevel. MÅLINGEN er til gengæld streng — kan den ikke læses, stopper
      # deployet.
      - name: Diskvagt og oprydning
        env:
          APP_DIR: ${{ inputs.app_dir }}
          IMAGE_REPO: ${{ env.APP_IMAGE_REPO }}
          IMAGE_KEEP: ${{ inputs.image_keep }}
          # Ét flag frem for `inputs.build_mode` i den fjerne streng: værdien
          # skal afgøres HER, hvor `build_mode`s betydning er kendt, ikke tolkes
          # af en anden maskines shell.
          REGISTRY_MODE: ${{ inputs.build_mode == 'registry' && '1' || '0' }}
          BUILDER_KEEP_HOURS: ${{ inputs.builder_keep_hours }}
          WARN_PCT: ${{ inputs.disk_warn_percent }}
          FAIL_PCT: ${{ inputs.disk_fail_percent }}
        run: |
          # pipefail er ikke valgfri: uden den ville `ssh … | tee` returnere
          # TEE's status, og en boks, der ikke svarer, ville se ud som en
          # vellykket måling — altså præcis det falsk-grønne, vagten findes for.
          set -euo pipefail
          # RUNNER_TEMP og ikke arbejdsmappen: `rsync app to server` synkroniserer
          # `sync_path` med --delete, så en rapportfil i træet ville blive lagt ud
          # på HVER apps boks og blive liggende. Målt på staging 16-08-2026.
          RAPPORT="${RUNNER_TEMP:-/tmp}/disk-rapport.txt"
          # ⚠️ VÆRDIERNE REJSER SOM NAVNGIVNE TILDELINGER PÅ STDIN — ikke som
          # positionsargumenter. Det er ikke stil, det er en ANVENDELSE af
          # reglen i "Citering ind i en fjern shell" øverst i filen, som
          # allerede har afvist den anden vej med rene ord: «`ssh … bash -s --
          # "$GIT_REF"`: ssh samler stadig argumenterne til én streng, som
          # login-shellen parser.» Diskvagten fra S-362 blev bygget på præcis
          # den kanal, noten frarådede.
          #
          # Hvad kanalen kostede, målt 20-08-2026 på en RIGTIG
          # staging-udrulning af area51 (S-357):
          #
          #     Diskvagt og oprydning
          #     bash: line 2: $4: unbound variable
          #
          # `IMAGE_REPO` kommer fra `needs.build.outputs.image_repo`, og
          # `build`-jobbet SPRINGES OVER i on-host-tilstand. Værdien er derfor
          # TOM pr. konstruktion for hver on-host-app. ssh samler sine
          # argumenter til ÉN streng, som den fjerne shell parser forfra: det
          # tomme argument forsvandt i fladningen, resten rykkede en plads ned,
          # `$4` blev aldrig sat, og `set -u` dræbte scriptet på linje 2.
          #
          # ssh-kommandoen er nu KONSTANT — `bash -s`, uden en eneste værdi i
          # sig. Det er hele virkningen: sshd kører `$SHELL -c <streng>`, og
          # den streng har ingenting at parse længere. Værdierne når aldrig dén
          # shell. De rejser på STDIN, foran kroppen, og læses kun af den bash,
          # de er skrevet til.
          #
          # NAVN frem for PLADS er dét, der lukker klassen — ikke citeringen:
          #   * en tom værdi bliver til `IMAGE_REPO=''` på sin egen linje. Den
          #     kan hverken forsvinde eller skubbe sine naboer, uanset hvad den
          #     indeholder.
          #   * der er ingen plads at rykke: S-365 hængte allerede en femte
          #     værdi på halen, og næste punkt, der skal sende én mere, kan
          #     ikke omnummerere de andre.
          #
          # `printf '%q'` alene (#65) gjorde tomheden SYNLIG som `''`, og det
          # virkede — men den lod kanalen stå, hvor argument N's betydning
          # afhænger af, at 1..N-1 alle overlevede fladningen. Her er der
          # ingen N. `%q` bliver stående som citering af selve værdien, jf.
          # filens egen regel; den bærer bare ikke længere tomheden alene.
          #
          # `-n "$IMAGE_REPO"`-vagten længere nede er RIGTIG og var der i
          # forvejen — den var bare uopnåelig, fordi tildelingen døde to linjer
          # tidligere. Formen her gør vagten nåelig; den erstatter den ikke.
          #
          # `${4:-}` ville have været ét tegn — og ville have gjort en højlydt
          # fejl tavs: ethvert argument kunne rykke sig, og en forkert værdi
          # lande i en rigtigt navngivet variabel. `set -u` er stadig vagten,
          # og den peger nu på et NAVN, ikke på et nummer.
          {
            printf 'APP_DIR=%q\n'            "$APP_DIR"
            printf 'IMAGE_REPO=%q\n'         "$IMAGE_REPO"
            printf 'IMAGE_KEEP=%q\n'         "$IMAGE_KEEP"
            printf 'REGISTRY_MODE=%q\n'      "$REGISTRY_MODE"
            printf 'BUILDER_KEEP_HOURS=%q\n' "$BUILDER_KEEP_HOURS"
            cat << 'REMOTE'
          set -euo pipefail
          # APP_DIR, IMAGE_REPO, IMAGE_KEEP, REGISTRY_MODE og
          # BUILDER_KEEP_HOURS er sat af de navngivne tildelinger, runneren
          # skrev foran denne krop — se noten i trinnet ovenfor. Mangler én,
          # dør `set -u` på DEN, der bruger den, og beskeden bærer variablens
          # navn i stedet for et argumentnummer.

          # Inddata afgøres FØRST, før noget som helst røres. En tastefejl i en
          # apps workflow skal fælde trinnet dér, hvor intet er fjernet endnu —
          # ikke vise sig som en tavs no-op nede i oprydningen, hvor `|| true`
          # ville sluge docker's klage over et flag, den ikke forstod.
          [ -n "$BUILDER_KEEP_HOURS" ] || BUILDER_KEEP_HOURS=0
          if ! printf '%s' "$BUILDER_KEEP_HOURS" | grep -Eq '^[0-9]+([.][0-9]+)?$'; then
            echo "builder_keep_hours skal være et tal i timer (0 slår reglen fra) — trinnet fik: '$BUILDER_KEEP_HOURS'" >&2
            exit 1
          fi
          # Tallet må gerne være en brøkdel af en time: prøven i
          # scripts/test-deploy-diskvagt-byggecache.sh kører reglen på
          # sekundskala, og det er dén, der gør den efterprøvbar mod en rigtig
          # dæmon uden at vente en uge.
          if awk -v t="$BUILDER_KEEP_HOURS" 'BEGIN { exit !(t > 0) }'; then
            BUILDER_KEEP_TIL=1
          else
            BUILDER_KEEP_TIL=0
          fi

          # Docker-roden er dét, et pull fylder op; app-mappen er dét, rsync og
          # backup fylder op. På dev-boksen er app-mappen et symlink til et
          # ANDET volume, så de to kan være hver sit filsystem — og ved et
          # førstedeploy findes mappen slet ikke endnu.
          DOCKER_ROOT="$(docker info -f '{{.DockerRootDir}}' 2>/dev/null || true)"
          [ -n "$DOCKER_ROOT" ] && [ -d "$DOCKER_ROOT" ] || DOCKER_ROOT=/
          [ -d "$APP_DIR" ] || APP_DIR=/

          # Det HØJESTE forbrug blandt dem — ikke gennemsnittet og ikke roden
          # alene. Ligger de to på samme filsystem, er svaret bare det samme.
          maal_pct() {
            hoejest=0
            for sti in "$DOCKER_ROOT" "$APP_DIR"; do
              p="$(df -P "$sti" | awk 'NR==2 {gsub(/%/,"",$5); print $5}')"
              if [ -n "$p" ] && [ "$p" -gt "$hoejest" ]; then hoejest="$p"; fi
            done
            echo "$hoejest"
          }

          # `sort -u` på selve linjen: er begge stier på samme filsystem, skal
          # rapporten ikke sige det samme to gange.
          vis_df() {
            { for sti in "$DOCKER_ROOT" "$APP_DIR"; do
                df -hP "$sti" | awk 'NR==2 {printf "  %-14s %5s brugt   %6s fri   (%s)\n", $6, $5, $4, $1}'
              done; } | sort -u
          }

          FOER="$(maal_pct)"
          echo "── Disk FØR oprydning"
          vis_df

          # 1) Appens EGNE images. Fredningen kommer fra MASKINEN, ikke fra
          #    alder: hvert image-ID, en container hænger på — kørende ELLER
          #    stoppet — beholdes, FØR der overhovedet tælles. En stoppet
          #    container er en bevidst valgt rollback-sti.
          FJERNET=0
          if [ "$REGISTRY_MODE" = "1" ] && [ -n "$IMAGE_REPO" ]; then
            BRUGTE="$(docker ps -aq | xargs -r docker inspect -f '{{.Image}}' 2>/dev/null | sort -u || true)"
            echo "── Images i '$IMAGE_REPO' (beholder alt i brug + $IMAGE_KEEP ubrugte)"
            BEHOLDT=0
            # --no-trunc i BEGGE ender: `docker inspect .Image` svarer med det
            # fulde sha256, mens `docker images` som standard forkorter til 12
            # tegn. Uden det ville fredningen aldrig matche, og trinnet ville
            # fjerne det image, containeren kører på.
            #
            # docker images svarer nyeste først; rækkefølgen ER "hvilke tre
            # ubrugte beholdes".
            while read -r ID REF; do
              [ -n "${ID:-}" ] || continue
              if printf '%s\n' "$BRUGTE" | grep -qxF "$ID"; then
                echo "  i brug    $REF"
                continue
              fi
              if [ "$BEHOLDT" -lt "$IMAGE_KEEP" ]; then
                BEHOLDT=$((BEHOLDT + 1))
                echo "  rollback  $REF"
                continue
              fi
              echo "  fjerner   $REF"
              if docker image rm "$REF" >/dev/null 2>&1; then
                FJERNET=$((FJERNET + 1))
              else
                echo "    (kunne ikke fjernes — springes over)"
              fi
            done < <(docker images --no-trunc --filter "reference=${IMAGE_REPO}" --format '{{.ID}} {{.Repository}}:{{.Tag}}')
            echo "  fjernet: $FJERNET"
          else
            echo "── Images: intet SHA-tagget repo at rydde i (build_mode: on-host)"
          fi

          # 2) Hængende images: UDEN -a, altså kun utaggede uden en container.
          #    Det er dødvægten fra et on-host-byg, hvor tagget flyttede med
          #    over på den nye udgave — og derfor det ene punkt, der også
          #    gavner en app, som ikke har valgt registry til.
          echo "── Hængende images"
          docker image prune -f 2>&1 | tail -n 1 || true

          # 3) Byggecache. To tilstande, to regler, fordi cachen betyder to
          #    forskellige ting de to steder — se afsnittet øverst i filen.
          #
          #    registry: boksen bygger ALDRIG, så alt kan gå (uændret).
          #
          #    on-host: cachen er dét, næste byg genbruger. `until=` snitter på
          #    SIDST BRUGT, så en post, byggene stadig rammer, nulstiller sit
          #    eget ur og ældes aldrig ud — derfor kan reglen være hård uden at
          #    ramme det varme. Den gælder først, når appen har sat inputtet.
          if [ "$REGISTRY_MODE" = "1" ]; then
            echo "── Byggecache (boksen bygger ikke i registry-tilstand)"
            docker builder prune -af 2>&1 | tail -n 1 || true
          elif [ "$BUILDER_KEEP_TIL" = "1" ]; then
            echo "── Byggecache ubrugt i over ${BUILDER_KEEP_HOURS} timer (alt nyere beholdes)"
            docker builder prune -f --filter "until=${BUILDER_KEEP_HOURS}h" 2>&1 | tail -n 1 || true
          else
            echo "── Byggecache: urørt — build_mode er on-host, og cachen er dét, næste byg genbruger (builder_keep_hours: 0)"
          fi

          EFTER="$(maal_pct)"
          echo "── Disk EFTER oprydning"
          vis_df
          echo "Diskforbrug: ${FOER}% → ${EFTER}%"

          # Maskinlæsbare linjer til sidst. Tærsklerne afgøres på RUNNEREN, hvor
          # de står i workflowets egen tekst, frem for inde i en fjern shell.
          echo "##DISK_PCT_FOER=${FOER}"
          echo "##DISK_PCT=${EFTER}"
          REMOTE
          } | ssh -i ~/.ssh/deploy_key \
                -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=20 \
                "root@${HOST}" bash -s | tee "$RAPPORT"

          PCT="$(sed -n 's/^##DISK_PCT=//p' "$RAPPORT" | tail -n 1)"
          FOER_PCT="$(sed -n 's/^##DISK_PCT_FOER=//p' "$RAPPORT" | tail -n 1)"

          # FEJLER LUKKET. Et manglende tal er ikke "alt er nok fint" — det er
          # "vi ved det ikke", og at rulle videre på dét er netop den antagelse,
          # der kostede en udrulning midt om natten.
          if ! [[ "$PCT" =~ ^[0-9]+$ ]]; then
            echo "::error title=Diskvagten kunne ikke måle ${DEPLOY_ENV}-boksen::Trinnet fik intet brugbart diskforbrug retur. Vagten fejler LUKKET frem for at rulle ud i blinde — se trinnets log ovenfor for, hvad boksen faktisk svarede."
            exit 1
          fi

          echo "Diskforbrug på ${DEPLOY_ENV} efter oprydning: ${PCT}% (før: ${FOER_PCT}%)"

          # Strengt større end, så disk_fail_percent: 100 slår stoppet fra uden
          # at slå målingen fra.
          if [ "$PCT" -gt "$FAIL_PCT" ]; then
            echo "::error title=For lidt plads på ${DEPLOY_ENV}-boksen (${PCT}%)::Disken er over ${FAIL_PCT}% EFTER oprydningen, så pullet ville sandsynligvis dø midt i udrulningen — det var dét, der væltede projects staging 16-08-2026 (S-362). INTET er rørt endnu: hverken backup, kode, skema eller containere. Se 'docker system df' på boksen for hvad der fylder."
            exit 1
          fi

          if [ "$PCT" -gt "$WARN_PCT" ]; then
            echo "::warning title=Disken på ${DEPLOY_ENV}-boksen fylder (${PCT}%)::Over ${WARN_PCT}% efter oprydningen. Udrulningen fortsætter, men der er ikke mange tilbage — se 'docker system df' på boksen."
          fi

      - name: Ensure app directory exists on persistent volume
        if: env.DEPLOY_ENV == 'dev' && inputs.ensure_dev_dir
        env:
          APP_DIR: ${{ inputs.app_dir }}
        run: |
          # `rm -rf` som root på den fjerne side — stien citeres, så et
          # mellemrum eller et metategn ikke kan blive til to argumenter.
          # Se "Citering ind i en fjern shell" øverst i filen.
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_DATA_DIR=$(printf '%q' "/mnt/ferm-data${APP_DIR}")
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" "
            mkdir -p ${Q_DATA_DIR}/pgdata
            mkdir -p ${Q_DATA_DIR}/data
            if [ ! -L ${Q_APP_DIR} ]; then
              rm -rf ${Q_APP_DIR}
              ln -sf ${Q_DATA_DIR} ${Q_APP_DIR}
            fi
          "

      - name: Backup database
        if: env.DEPLOY_ENV != 'dev'
        # Trinnet valgte før db_container UBETINGET — altså prod-navnet, også på
        # staging. SQL-trinnet nedenfor valgte allerede rigtigt mellem de tre
        # inputs; backup'en brugte bare ikke mekanismen. Resultatet: projects
        # staging ledte efter ferm-projects-db, fandt den ikke (den hedder
        # ferm-project-staging-db), skrev "(første deploy?)" og gik grønt —
        # gennem 210 push-deploys uden backup mellem 16-07 og 08-08-2026 (S-211).
        #
        # Derfor skelner trinnet nu mellem to slags "ingen backup", som før så ens ud:
        #   * INGEN db-container for appen kører        → ægte førstedeploy, spring over (exit 0).
        #   * Containeren findes under et ANDET navn,
        #     eller findes og er stoppet                → opsætningsfejl, stop deployet (exit 1)
        #                                                 FØR det rører databasen.
        # "Er navnet konfigureret?" duer ikke som skel: crm og devhub sætter
        # ingen override (deres staging-db hedder tilfældigvis det samme som
        # prods), så en fremtidig omdøbning dér ville give præcis S-211 igen uden
        # at nogen havde konfigureret noget forkert. Beviset skal komme fra
        # maskinen, ikke fra inputtet.
        #
        # En pg_dump der FEJLER er heller ikke en gyldig overspringning: uden
        # pipefail returnerer `pg_dump | gzip > fil` 0 og efterlader en ~20 bytes
        # tom .gz, og deployet ruller videre i troen på at der er taget backup.
        # Hærdet udgave fra infrastructure-templaten (#214).
        run: |
          BACKUP_DIR=$([[ "$DEPLOY_ENV" == "production" ]] && echo "/var/backups/prod" || echo "/var/backups/staging")
          BACKUP_LABEL=$([[ "$DEPLOY_ENV" == "production" ]] && echo "prod" || echo "staging")
          # db_container er prod-navnet; dev og staging har typisk hver sit.
          # Samme mønster som "Run SQL scripts" — rettes den ene, skal den anden med.
          DB_CONTAINER="${{ inputs.db_container }}"
          if [[ "$DEPLOY_ENV" == "staging" && -n "${{ inputs.staging_db_container }}" ]]; then
            DB_CONTAINER="${{ inputs.staging_db_container }}"
          elif [[ "$DEPLOY_ENV" == "dev" && -n "${{ inputs.dev_db_container }}" ]]; then
            DB_CONTAINER="${{ inputs.dev_db_container }}"
          fi
          # MODE=skip-if-absent: et deploy af en app, der ikke er sat op endnu,
          # skal kunne køre igennem. Den planlagte backup (backup-db.yml) sender
          # MODE=require i stedet — dér betyder "ingen database" ikke
          # førstedeploy, men at en app i drift er væk, og det skal larme.
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            bash -s -- "$BACKUP_DIR" "$BACKUP_LABEL" "$DB_CONTAINER" \
                       "${{ inputs.app_slug }}" "${{ inputs.db_user }}" "${{ inputs.db_name }}" \
                       skip-if-absent << 'REMOTE'
          set -euo pipefail
          BACKUP_DIR="$1"; BACKUP_LABEL="$2"; DB_CONTAINER="$3"
          APP_SLUG="$4"; DB_USER="$5"; DB_NAME="$6"; MODE="${7:-skip-if-absent}"

          # Læs listen ÉN gang i en variabel og match med here-strings.
          # `docker ps | grep -q` er ikke sikkert her: grep -q afslutter ved
          # første match, docker kan nå at få SIGPIPE, og med `pipefail` bliver
          # hele pipelinen så non-zero — altså ser en KØRENDE container fraværende
          # ud. Det er sjældent og tidsafhængigt, og netop derfor uacceptabelt i
          # et trin, hvis eneste opgave er at afgøre præcis det spørgsmål.
          RUNNING="$(docker ps --format '{{.Names}}')"

          if ! grep -qxF "$DB_CONTAINER" <<< "$RUNNING"; then
            # Kører en ANDEN db-container for appen? Så er navnet forkert for
            # dette miljø — netop S-211 — og det skal larme, ikke springes over.
            # Slug'en matches med index(), ikke som regex, så et punktum eller
            # plus i et app_slug ikke stille udvider hvad der tæller som match.
            # Det tjek kommer FØRST: står der en gammel, stoppet container under
            # prod-navnet, mens den rigtige kører under et andet, er "du har
            # peget på det forkerte navn" den brugbare besked — ikke "start den".
            OTHERS="$(awk -v s="$APP_SLUG" 'index($0, s) && /-db$/' <<< "$RUNNING")"
            if [ -n "$OTHERS" ]; then
              echo "DB-containeren '$DB_CONTAINER' kører ikke, men disse db-containere for '$APP_SLUG' gør:" >&2
              sed 's/^/  /' <<< "$OTHERS" >&2
              echo "Miljøet ($BACKUP_LABEL) peger på det forkerte navn — ingen backup taget, deployet stoppet." >&2
              echo "Sæt staging_db_container / dev_db_container i appens deploy.yml." >&2
              exit 1
            fi
            # Findes den, men kører ikke? Så er stakken sat op før, og det er
            # ikke et førstedeploy — det er en nede database.
            if grep -qxF "$DB_CONTAINER" <<< "$(docker ps -a --format '{{.Names}}')"; then
              echo "DB-containeren '$DB_CONTAINER' findes, men kører ikke — kan ikke tage backup." >&2
              echo "Start den (docker start $DB_CONTAINER) og kør deployet igen." >&2
              exit 1
            fi
            # Ingen db-container for appen overhovedet: stakken findes ikke endnu.
            # Ved et deploy er det et førstedeploy og helt i orden. Ved en
            # PLANLAGT backup er det ikke: appen er i drift, og at dens database
            # ikke kører er præcis det, en backup-cron skal opdage. Samme tekst
            # ville dér betyde "alt vel" om noget, der er galt.
            if [ "$MODE" = "require" ]; then
              echo "Ingen kørende db-container for '$APP_SLUG' ('$DB_CONTAINER' findes ikke)." >&2
              echo "Ved en planlagt backup er det ikke et førstedeploy — der blev ikke taget backup." >&2
              exit 1
            fi
            echo "Ingen kørende db-container for '$APP_SLUG' ('$DB_CONTAINER' findes ikke) — førstedeploy, springer backup over."
            exit 0
          fi

          mkdir -p "$BACKUP_DIR"
          OUT="${BACKUP_DIR}/${APP_SLUG}-${BACKUP_LABEL}-$(date +%Y%m%d_%H%M%S).sql.gz"
          docker exec "$DB_CONTAINER" pg_dump -U "$DB_USER" "$DB_NAME" | gzip > "$OUT"
          # gzip af en tom stream giver stadig ~20 bytes — tjek indholdet, ikke filen.
          if [ "$(gzip -dc "$OUT" | head -c 1 | wc -c)" -eq 0 ]; then
            echo "Backup $OUT er tom — pg_dump gav intet output" >&2
            exit 1
          fi
          echo "Backup: $OUT ($(du -h "$OUT" | cut -f1))"
          REMOTE

      - name: rsync app to server
        run: |
          EXTRA_EXCLUDES=""
          for e in ${{ inputs.extra_excludes }}; do
            EXTRA_EXCLUDES="$EXTRA_EXCLUDES --exclude $e"
          done
          rsync -az --delete \
            --exclude 'node_modules' \
            --exclude '.next' \
            --exclude '.git' \
            --exclude '.env*' \
            --exclude '.DS_Store' \
            $EXTRA_EXCLUDES \
            -e "ssh -i ~/.ssh/deploy_key -o BatchMode=yes" \
            ${{ inputs.sync_path }} \
            "root@${HOST}:${{ inputs.app_dir }}/"

      # ── Strategi: up-build ──────────────────────────────────────────────────
      - name: Build and restart containers
        if: inputs.deploy_strategy == 'up-build'
        env:
          # NPM_READ_TOKEN er org'ens levende læse-token; NPM_TOKEN døde udløbet
          # 2026-07-23 og gav 401 i crm's build (eneste app der rammer registret
          # — resten vendorer). Shell-navnet forbliver NPM_TOKEN, for det er det
          # Dockerfiles/.npmrc forventer.
          NPM_TOKEN: ${{ secrets.NPM_READ_TOKEN || secrets.NPM_TOKEN }}
          APP_DIR: ${{ inputs.app_dir }}
        run: |
          # Citeret til den FJERNE shell — se "Citering ind i en fjern shell"
          # øverst i filen. GIT_REF er et branchnavn og må indeholde `'`.
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_GIT_SHA=$(printf '%q' "$GIT_SHA")
          Q_GIT_REF=$(printf '%q' "$GIT_REF")
          Q_NPM_TOKEN=$(printf '%q' "$NPM_TOKEN")
          ssh -i ~/.ssh/deploy_key \
            -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=20 \
            "root@${HOST}" \
            "docker network create ferm-net 2>/dev/null || true && cd ${Q_APP_DIR} && export GIT_SHA=${Q_GIT_SHA} GIT_REF=${Q_GIT_REF} && NPM_TOKEN=${Q_NPM_TOKEN} docker compose ${COMPOSE_FLAGS} up -d --build --force-recreate --remove-orphans"

      # ── Strategier: build-up og migrate-first ───────────────────────────────
      # Bygger kun imaget. Ved migrate-first hænger den kørende container på sit
      # gamle image-ID og røres ikke endnu.
      - name: Build image
        if: inputs.deploy_strategy != 'up-build' && inputs.build_mode != 'registry'
        env:
          # NPM_READ_TOKEN er org'ens levende læse-token; NPM_TOKEN døde udløbet
          # 2026-07-23 og gav 401 i crm's build (eneste app der rammer registret
          # — resten vendorer). Shell-navnet forbliver NPM_TOKEN, for det er det
          # Dockerfiles/.npmrc forventer.
          NPM_TOKEN: ${{ secrets.NPM_READ_TOKEN || secrets.NPM_TOKEN }}
          APP_DIR: ${{ inputs.app_dir }}
        run: |
          BUILD_FLAGS=$([[ "$DEPLOY_ENV" == "production" ]] && echo "--no-cache --pull" || echo "--pull")
          # Citeret til den FJERNE shell — se "Citering ind i en fjern shell"
          # øverst i filen. GIT_REF er et branchnavn og må indeholde `'`.
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_GIT_SHA=$(printf '%q' "$GIT_SHA")
          Q_GIT_REF=$(printf '%q' "$GIT_REF")
          Q_NPM_TOKEN=$(printf '%q' "$NPM_TOKEN")
          ssh -i ~/.ssh/deploy_key \
            -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=20 \
            "root@${HOST}" \
            "docker network create ferm-net 2>/dev/null || true && cd ${Q_APP_DIR} && export GIT_SHA=${Q_GIT_SHA} GIT_REF=${Q_GIT_REF} && NPM_TOKEN=${Q_NPM_TOKEN} docker compose ${COMPOSE_FLAGS} build ${BUILD_FLAGS}"

      # ── Registry: hent imaget i stedet for at bygge det ────────────────────
      # Erstatter trinnet ovenfor. Nedetiden falder fra "byggeriet udsulter
      # boksen" til "et pull, mens den gamle container kører videre".
      #
      # Login, pull og oprydning ligger i ÉT trin med vilje. Nøglen skal ligge
      # på maskinen i så kort tid som muligt, og alt efter pullet (drift-gate,
      # db push, `up -d`) bruger et image, der allerede er lokalt —
      # registret er der ikke brug for igen. Lå oprydningen til sidst i jobbet,
      # var vinduet hele udrulningen i stedet for de sekunder, et pull tager.
      #
      # Tre ting om hemmeligheden, og de er alle tre bevidste:
      #   * Tokenet går over STDIN, aldrig i argv — en kommandolinje kan læses
      #     af enhver med adgang til boksens procesliste.
      #   * EGEN docker-config-mappe pr. app (`DOCKER_CONFIG`). Boksene deles:
      #     dev kører project + crm + area51, og `concurrency`-gruppen er pr.
      #     app, så to udrulninger kan overlappe. Med den fælles
      #     /root/.docker/config.json ville den enes `docker logout` rive den
      #     andens nøgle væk MIDT i et pull — en flaky 401, ingen kunne
      #     forklare — og mens begge var logget ind, ville den sidste login
      #     gælde for dem begge.
      #   * Mappen SLETTES frem for at blive `docker logout`'et. En sletning kan
      #     ikke efterlade en post, og `trap` sikrer, at den også sker, når
      #     pullet fejler.
      - name: Pull image on server
        if: inputs.build_mode == 'registry'
        env:
          # Via `env:` frem for `${{ }}` direkte i kommandostrengen. Actions
          # interpolerer `${{ }}` som TEKST, før bash ser linjen, så en værdi med
          # et citationstegn ville kunne bryde ud og køre som root på boksen.
          # Som miljøvariabler udvides de af runnerens bash i stedet.
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APP_DIR: ${{ inputs.app_dir }}
          ACTOR: ${{ github.actor }}
          SERVICE: ${{ inputs.compose_service }}
          SLUG: ${{ inputs.app_slug }}
        run: |
          # Tokenet er det eneste, der går over stdin — `docker login
          # --password-stdin` er den første og eneste læser.
          #
          # Det afsluttende `docker image inspect` er VÆRNET mod et tavst byg på
          # boksen: imaget skal ligge lokalt, når pullet er færdigt. Uden den
          # påstand ville et pull, der ikke gav det forventede tag, først vise
          # sig i drift-gaten — og dér BYGGER `docker compose run` imaget af sig
          # selv, hvis det mangler. Altså præcis den nedetid, S-349 fjerner, på
          # det trin der er sværest at få øje på.
          #
          # En påstand og ikke et `--no-build`-flag: `docker compose run` kender
          # ikke det flag (målt 15-08-2026 på staging-boksen: `unknown flag:
          # --no-build`) — kun `up` gør. En eksistenspåstand virker på enhver
          # compose-version og fejler desuden tidligere.
          #
          # Kommentarer holdes UDEN FOR den fjerne kommandostreng med vilje:
          # alt inde i den er tekst, en anden maskines shell skal parse.
          # Alt, der ikke er en kommando, citeres til den FJERNE shell — se
          # "Citering ind i en fjern shell" øverst i filen.
          Q_SLUG=$(printf '%q' "$SLUG")
          Q_ACTOR=$(printf '%q' "$ACTOR")
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_APP_IMAGE=$(printf '%q' "$APP_IMAGE")
          Q_SERVICE=$(printf '%q' "$SERVICE")
          printf '%s' "$GHCR_TOKEN" | ssh -i ~/.ssh/deploy_key \
            -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=20 \
            "root@${HOST}" \
            "set -e
             export DOCKER_CONFIG=/root/.docker-deploy-${Q_SLUG}
             trap 'rm -rf \"\$DOCKER_CONFIG\"' EXIT
             install -d -m 700 \"\$DOCKER_CONFIG\"
             docker login ghcr.io -u ${Q_ACTOR} --password-stdin
             docker network create ferm-net 2>/dev/null || true
             cd ${Q_APP_DIR}
             export APP_IMAGE=${Q_APP_IMAGE}
             docker compose ${COMPOSE_FLAGS} pull ${Q_SERVICE}
             docker image inspect ${Q_APP_IMAGE} >/dev/null"

      # Porten (devhub #25). Diff'en beregnes med det NYE image (skemaet er bagt
      # ind) mod den KØRENDE database. Er ændringen tabsgivende, dør deployet her
      # — før noget er genstartet.
      - name: Gate — schema drift må ikke være tabsgivende
        if: inputs.deploy_strategy == 'migrate-first' && inputs.schema_drift_gate
        env:
          APP_DIR: ${{ inputs.app_dir }}
          SERVICE: ${{ inputs.compose_service }}
        run: |
          # APP_IMAGE eksporteres i HVERT compose-kald, ikke kun ved pull.
          # Compose-servicen slår op i `${APP_IMAGE:-<lokalt navn>}`, så en
          # glemt eksport ville tavst køre engangscontaineren på det GAMLE,
          # lokale image — altså regne driften på et andet skema end det, der
          # skal rulles ud. I on-host-tilstand er variablen tom, og `:-`
          # leverer det lokale navn som hidtil.
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_APP_IMAGE=$(printf '%q' "${APP_IMAGE:-}")
          Q_SERVICE=$(printf '%q' "$SERVICE")
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            "cd ${Q_APP_DIR} && export APP_IMAGE=${Q_APP_IMAGE} && \
             docker compose ${COMPOSE_FLAGS} run --rm ${Q_SERVICE} \
               npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script" \
            > drift.sql
          echo "::group::Skema-drift ($DEPLOY_ENV)"
          cat drift.sql
          echo "::endgroup::"
          node scripts/schema-drift.mjs < drift.sql

      # Migrér FØR genstart i en engangscontainer — den kørende container er urørt.
      - name: Run DB migrations (before restart)
        if: inputs.deploy_strategy == 'migrate-first'
        env:
          APP_DIR: ${{ inputs.app_dir }}
          SERVICE: ${{ inputs.compose_service }}
        run: |
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_APP_IMAGE=$(printf '%q' "${APP_IMAGE:-}")
          Q_SERVICE=$(printf '%q' "$SERVICE")
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            "cd ${Q_APP_DIR} && export APP_IMAGE=${Q_APP_IMAGE} && \
             docker compose ${COMPOSE_FLAGS} run --rm ${Q_SERVICE} \
               npx prisma db push"

      # Umiddelbart efter db push og FØR genstart — samme grund som selve
      # migrate-first-strategien: den nye kode må aldrig møde et halvfærdigt
      # skema. En CHECK-constraint er skemaarbejde på lige fod med en kolonne.
      #
      # NB: body'en er med vilje IDENTISK med "Run SQL scripts" nedenfor. De to
      # kan ikke slås sammen (de skal ligge hver sit sted i rækkefølgen), og et
      # composite action ville koste mere end de sparer: runneren henter ALLE
      # actions i "Set up job", før noget `if:` er evalueret, så en
      # `uses: ...@main` ville dels ikke kunne testes fra en branch (den findes
      # først på main efter merge), dels vælte hver eneste apps deploy — også
      # dem uden scripts/sql — hvis mappen nogensinde blev flyttet. Rettes den
      # ene, skal den anden med.
      - name: Run SQL scripts (before restart)
        if: inputs.deploy_strategy == 'migrate-first'
        run: |
          # db_container er prod-navnet; dev og staging har typisk hver sit.
          DB_CONTAINER="${{ inputs.db_container }}"
          if [[ "$DEPLOY_ENV" == "staging" && -n "${{ inputs.staging_db_container }}" ]]; then
            DB_CONTAINER="${{ inputs.staging_db_container }}"
          elif [[ "$DEPLOY_ENV" == "dev" && -n "${{ inputs.dev_db_container }}" ]]; then
            DB_CONTAINER="${{ inputs.dev_db_container }}"
          fi
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            bash -s -- "${{ inputs.app_dir }}" "$DB_CONTAINER" \
                       "${{ inputs.db_user }}" "${{ inputs.db_name }}" << 'REMOTE'
          set -euo pipefail
          APP_DIR="$1"; DB_CONTAINER="$2"; DB_USER="$3"; DB_NAME="$4"
          SQL_DIR="$APP_DIR/scripts/sql"

          if [ ! -d "$SQL_DIR" ]; then
            echo "Ingen $SQL_DIR — intet SQL at køre."
            exit 0
          fi

          # nullglob: en tom mappe skal give en tom liste, ikke den bogstavelige
          # streng "*.sql", som psql så ville fejle på.
          shopt -s nullglob
          FILES=("$SQL_DIR"/*.sql)
          shopt -u nullglob

          if [ ${#FILES[@]} -eq 0 ]; then
            echo "Ingen .sql-filer i $SQL_DIR — intet at køre."
            exit 0
          fi

          # Først her: apps uden scripts/sql skal ikke fejle på en container,
          # de aldrig får brug for.
          if ! docker inspect "$DB_CONTAINER" >/dev/null 2>&1; then
            echo "DB-containeren '$DB_CONTAINER' findes ikke i dette miljø."
            echo "Sæt dev_db_container / staging_db_container i appens deploy.yml."
            exit 1
          fi

          # LC_ALL=C: samme rækkefølge uanset serverens locale, så et 010-/020-
          # præfiks også styrer rækkefølgen på en boks med andet sprog.
          IFS=$'\n' FILES=($(printf '%s\n' "${FILES[@]}" | LC_ALL=C sort))

          for f in "${FILES[@]}"; do
            echo "── $(basename "$f")"
            # ON_ERROR_STOP=1 er IKKE valgfri: uden den fortsætter psql efter en
            # fejl og afslutter med 0, så et brækket script ville rulle videre
            # som om alt gik godt — endnu et falsk-grønt signal.
            # --single-transaction gør hver fil atomisk, så et script der fejler
            # halvvejs ikke efterlader sine første statements committet.
            docker exec -i "$DB_CONTAINER" \
              psql -v ON_ERROR_STOP=1 --single-transaction -U "$DB_USER" -d "$DB_NAME" < "$f"
          done

          echo "${#FILES[@]} SQL-script(s) kørt."
          REMOTE

      - name: Start containers
        if: inputs.deploy_strategy != 'up-build'
        env:
          APP_DIR: ${{ inputs.app_dir }}
        run: |
          UP_FLAGS=$([[ "$DEPLOY_ENV" == "production" ]] && echo "--force-recreate" || echo "")
          # Bemærk: intet `--no-build` her. Værnet mod et tavst on-host-byg er
          # eksistenspåstanden i "Pull image on server" — den virker på enhver
          # compose-version og fejler tidligere. Se noten dér.
          # Citeret til den FJERNE shell — se "Citering ind i en fjern shell"
          # øverst i filen. GIT_REF er et branchnavn og må indeholde `'`.
          Q_APP_DIR=$(printf '%q' "$APP_DIR")
          Q_GIT_SHA=$(printf '%q' "$GIT_SHA")
          Q_GIT_REF=$(printf '%q' "$GIT_REF")
          Q_APP_IMAGE=$(printf '%q' "${APP_IMAGE:-}")
          ssh -i ~/.ssh/deploy_key \
            -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=20 \
            "root@${HOST}" \
            "cd ${Q_APP_DIR} && export GIT_SHA=${Q_GIT_SHA} GIT_REF=${Q_GIT_REF} APP_IMAGE=${Q_APP_IMAGE} && docker compose ${COMPOSE_FLAGS} up -d ${UP_FLAGS}"

      # Kør db push INDE i app-containeren: dér findes den pinnede prisma-CLI,
      # prisma.config.ts og DATABASE_URL allerede. En nøgen node-container har
      # ingen node_modules, så npx trækker nyeste prisma — og en major-version
      # kan (og har) brækket deployet tavst.
      - name: Run DB migrations
        if: inputs.deploy_strategy != 'migrate-first'
        env:
          APP_CONTAINER_PROD: ${{ inputs.app_container }}
          APP_CONTAINER_STAGING: ${{ inputs.staging_app_container }}
        run: |
          APP_CONTAINER="$APP_CONTAINER_PROD"
          if [[ "$DEPLOY_ENV" == "staging" && -n "$APP_CONTAINER_STAGING" ]]; then
            APP_CONTAINER="$APP_CONTAINER_STAGING"
          fi
          Q_APP_CONTAINER=$(printf '%q' "$APP_CONTAINER")
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            "docker exec ${Q_APP_CONTAINER} npx prisma db push"

      # De gamle strategier migrerer først EFTER genstart — det er hele grunden
      # til at migrate-first findes. SQL'en følger db push, hvor den nu end
      # ligger, så rækkefølgen skema-før-SQL holder i begge tilfælde.
      # Identisk body med "Run SQL scripts (before restart)" ovenfor — se noten
      # dér om hvorfor de to ikke er ét composite action.
      - name: Run SQL scripts
        if: inputs.deploy_strategy != 'migrate-first'
        run: |
          # db_container er prod-navnet; dev og staging har typisk hver sit.
          DB_CONTAINER="${{ inputs.db_container }}"
          if [[ "$DEPLOY_ENV" == "staging" && -n "${{ inputs.staging_db_container }}" ]]; then
            DB_CONTAINER="${{ inputs.staging_db_container }}"
          elif [[ "$DEPLOY_ENV" == "dev" && -n "${{ inputs.dev_db_container }}" ]]; then
            DB_CONTAINER="${{ inputs.dev_db_container }}"
          fi
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            bash -s -- "${{ inputs.app_dir }}" "$DB_CONTAINER" \
                       "${{ inputs.db_user }}" "${{ inputs.db_name }}" << 'REMOTE'
          set -euo pipefail
          APP_DIR="$1"; DB_CONTAINER="$2"; DB_USER="$3"; DB_NAME="$4"
          SQL_DIR="$APP_DIR/scripts/sql"

          if [ ! -d "$SQL_DIR" ]; then
            echo "Ingen $SQL_DIR — intet SQL at køre."
            exit 0
          fi

          shopt -s nullglob
          FILES=("$SQL_DIR"/*.sql)
          shopt -u nullglob

          if [ ${#FILES[@]} -eq 0 ]; then
            echo "Ingen .sql-filer i $SQL_DIR — intet at køre."
            exit 0
          fi

          # Først her: apps uden scripts/sql skal ikke fejle på en container,
          # de aldrig får brug for.
          if ! docker inspect "$DB_CONTAINER" >/dev/null 2>&1; then
            echo "DB-containeren '$DB_CONTAINER' findes ikke i dette miljø."
            echo "Sæt dev_db_container / staging_db_container i appens deploy.yml."
            exit 1
          fi

          IFS=$'\n' FILES=($(printf '%s\n' "${FILES[@]}" | LC_ALL=C sort))

          for f in "${FILES[@]}"; do
            echo "── $(basename "$f")"
            docker exec -i "$DB_CONTAINER" \
              psql -v ON_ERROR_STOP=1 --single-transaction -U "$DB_USER" -d "$DB_NAME" < "$f"
          done

          echo "${#FILES[@]} SQL-script(s) kørt."
          REMOTE

      # (Registry-nøglen ryddes i "Pull image on server" — se `trap` dér. Der er
      # med vilje IKKE et logout-trin her til sidst: nøglen skal ikke ligge på
      # boksen gennem drift-gate, migrationer og genstart, når intet efter
      # pullet har brug for registret.)

      - name: Reload Caddy
        if: inputs.reload_caddy == 'always' || (inputs.reload_caddy == 'non-dev' && inputs.environment != 'dev')
        run: |
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "root@${HOST}" \
            "docker exec ferm-caddy caddy reload --config /etc/caddy/Caddyfile"

      # Rapportér deployet til DevHub (devhubs mønster, docs/version-patch.md).
      # Må ALDRIG vælte et deploy — continue-on-error + '|| true', og springes
      # over hvis secrets ikke er sat i repoet.
      - name: Report deployment to DevHub
        if: always()
        continue-on-error: true
        env:
          DEVHUB_URL: ${{ secrets.DEVHUB_URL }}
          DEVHUB_DEPLOY_TOKEN: ${{ secrets.DEVHUB_DEPLOY_TOKEN }}
          DEPLOY_STATUS: ${{ job.status }}
        run: |
          [ -n "$DEVHUB_URL" ] && [ -n "$DEVHUB_DEPLOY_TOKEN" ] || { echo "DevHub reporting not configured — skipping"; exit 0; }
          curl -fsS -m 10 -X POST "$DEVHUB_URL/api/deployments" \
            -H "authorization: Bearer $DEVHUB_DEPLOY_TOKEN" \
            -H "content-type: application/json" \
            -d "$(printf '{"app":"%s","env":"%s","sha":"%s","ref":"%s","status":"%s","actor":"%s","runUrl":"%s"}' \
                  "${{ inputs.app_slug }}" "$DEPLOY_ENV" "$GIT_SHA" "$GIT_REF" "$DEPLOY_STATUS" \
                  "${{ github.actor }}" \
                  "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}")" \
            || true
