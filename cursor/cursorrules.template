# Project context for Cursor AI

## What this project is
TODO: one paragraph description of what this codebase does.

## Stack
TODO: list the key technologies (e.g. Next.js 14, PostgreSQL, Prisma, TypeScript).

## Folder structure
TODO:
```
src/
  app/       — Next.js App Router pages and API routes
  lib/       — shared utilities and helpers
  components/ — React components
```

## Key conventions
- TODO: e.g. "Use server components by default; add 'use client' only when needed"
- TODO: e.g. "All DB queries go through the repository layer in lib/db/"
- TODO: e.g. "Error responses always use { error: string } shape"

## Commands
```bash
npm run dev       # start dev server
npm test          # run tests
npm run typecheck # tsc --noEmit
npm run lint      # eslint
```

## What to avoid
- TODO: e.g. "Never use any — use unknown and narrow"
- TODO: e.g. "Don't add console.log to production code"
- TODO: e.g. "Don't modify the generated Prisma client files"

## Auth
TODO: describe how auth works (session cookies, tokens, middleware checks).
